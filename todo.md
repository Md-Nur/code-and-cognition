There have two components folder one is outside app directory one is inside app directory. rearrage these file with best practices

When a new insights publish all the subcriber should be emailed.    

Case study must be sort descending by date.

when navigate to /insights/[slug] from other page their have a huge delay. Try to reduce the delay and add the loading component at that time. 